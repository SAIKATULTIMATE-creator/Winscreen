{
  "scripts": {
    "build:mobile": "npm run build",
    "cap:sync": "npm run build:mobile && npx cap sync",
    "cap:open": "npx cap open android",
    "build:apk": "npm run cap:sync && npx cap open android"
  }
}